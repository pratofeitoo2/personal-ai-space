[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "appointment_mcp"

[project]
name = "appointment-mcp"
version = "0.1.0"
description = "MCP server for appointment/calendar access"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"

dependencies = [
    "fastmcp>=2.0",
    "pydantic>=2.0",
]

[project.scripts]
appointment-mcp = "appointment_mcp:serve"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]